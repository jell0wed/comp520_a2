// This should not work because the initialized value of atest
// does not resolve to a string. Hence it shouldnt pass the typechecking phase

var atest: string = (1+2 - 0.5); // should resolve to a float

print atest; // should not work